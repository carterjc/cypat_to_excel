# cypat_to_excel
Tool that scrapes the CyberPatriot public scoreboard and dumps the data into an excel file.

# Synopsis
This program will generate a detailed Excel file that can be used as one pleases. Be warned, this program takes a particularly long time to run (especially in the early rounds) because it makes thousands of individual get requests.
